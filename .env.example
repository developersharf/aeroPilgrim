DEBUG=True
SECRET_KEY=change-me

# Comma-separated hosts (Railway domain, custom domain, etc.)
ALLOWED_HOSTS=localhost,127.0.0.1
# If you enable HTTPS + CSRF checks behind a domain:
# CSRF_TRUSTED_ORIGINS=https://your-app.up.railway.app

# RapidAPI Sky Scrapper
FLIGHT_API_KEY=
FLIGHT_API_HOST=sky-scrapper.p.rapidapi.com

# Optional AI
OPENAI_API_KEY=
AI_MODEL=gpt-4o-mini

# Optional Postgres (Railway provides DATABASE_URL automatically if you add Postgres)
# DATABASE_URL=postgresql://user:pass@host:5432/dbname
